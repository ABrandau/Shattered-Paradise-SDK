World:
	-SpawnStartingUnits:
	-MapStartingLocations:
	MapBuildRadius:
		AllyBuildRadiusCheckboxLocked: true
		AllyBuildRadiusCheckboxEnabled: true
		BuildRadiusCheckboxLocked: true
		BuildRadiusCheckboxEnabled: true
	MapOptions:
		TechLevelDropdownLocked: true
		ShortGameCheckboxLocked: true
		ShortGameCheckboxEnabled: true
	CrateSpawner:
		CheckboxEnabled: false
		CheckboxLocked: true
	LuaScript:
		Scripts: lua/utils.lua, lua/players.lua, lua/objectives.lua, lua/actors.lua, lua/world.lua, mission.lua
	ObjectivesPanel:
		PanelName: MISSION_OBJECTIVES

Player:
	-ConquestVictoryConditions:
	MissionObjectives:
		EarlyGameOver: true
	EnemyWatcher:
	Shroud:
		FogCheckboxLocked: true
		FogCheckboxEnabled: true
		ExploredMapCheckboxLocked: true
		ExploredMapCheckboxEnabled: false
	PlayerResources:
		DefaultCashDropdownLocked: true
		DefaultCash: 5000
	##AI
	## Use Easy AI for basic AI logic like load and unload,
	## so we remove the builder and squad manager from Easy AI
	## Tip: Easy AI + Lua for level that we need to script the AI
	SharedCargoBotModule:
		Transports: muvent, muventi, mutambushvent ##add mutambushvent
	UnitBuilderBotModule@AIAll:
		RequiresCondition: AIBase
	BaseBuilderBotModule@AIBase:
		RequiresCondition: AIBase
	SquadManagerBotModule@AIBase:
		RequiresCondition: AIBase
	CncEngineerBotModule:
		RequiresCondition: AIBase
	McvManagerASBotModule@AINormal:
		RequiresCondition: AIBase
	McvManagerASBotModule@mobilewarfactory-control:
		RequiresCondition: AIBase

